<?xml version="1.0" encoding="utf-8"?>
<!-- 取消按钮效果 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 选中未up时 -->
    <item android:state_pressed="true"><shape>
            <!--颜色渐变startColor:开始颜色endColor:结束颜色angle:渐变方向   -->
            <gradient android:endColor="@color/colorPrimary" android:gradientRadius="500" android:startColor="#EAEAEA" android:type="radial" />
            <!--按钮四个圆角radius:半径 -->
            <corners android:bottomLeftRadius="3dp" android:bottomRightRadius="3dp" android:topLeftRadius="3dp" android:topRightRadius="3dp" />
            <!-- 按钮文字和边缘距离(内边距) -->
            <!-- <padding android:bottom="10dp" android:left="10dp" android:right="10dp" android:top="10dp" /> -->
        </shape>
    </item>

    <!-- 焦点 -->
    <item android:state_focused="true">
        <shape>
            <gradient android:endColor="@color/colorPrimary" android:gradientRadius="500" android:startColor="#EAEAEA" android:type="radial" />
            <corners android:bottomLeftRadius="3dp" android:bottomRightRadius="3dp" android:topLeftRadius="3dp" android:topRightRadius="3dp" />
            <!-- <padding android:bottom="10dp" android:left="10dp" android:right="10dp" android:top="10dp" /> -->
        </shape>
    </item>

    <!-- 无焦点 -->
    <item>
        <shape>
            <gradient android:endColor="@color/colorPrimary" android:startColor="@color/colorPrimary"/>
            <corners android:bottomLeftRadius="3dp" android:bottomRightRadius="3dp" android:topLeftRadius="3dp" android:topRightRadius="3dp" />
            <!-- <padding android:bottom="10dp" android:left="10dp" android:right="10dp" android:top="10dp" /> -->
        </shape>
    </item>

</selector>